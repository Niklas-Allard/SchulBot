services:
  schulbot:
    build: .
    restart: unless-stopped
    command: ["./devtools-linux-amd64"]
    env_file: .env
    volumes:
      - schulbot-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  schulbot-data:
